services:
  - name: smoking-app
    type: web
    env: python
    plan: free
    runtime: python
    pythonVersion: "3.10.12"
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn app:app"
